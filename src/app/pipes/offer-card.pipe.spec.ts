import { OfferCardPipe } from './offer-card.pipe';

describe('OfferCardPipe', () => {
  it('create an instance', () => {
    const pipe = new OfferCardPipe();
    expect(pipe).toBeTruthy();
  });
});
